services:
  api:
    build: .
    ports: [ "8080:8080" ]
    environment:
      PGVECTOR_DSN: postgresql://ai:ai@postgres:5432/rag
    depends_on: [ postgres ]
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: ai
      POSTGRES_PASSWORD: ai
      POSTGRES_DB: rag
